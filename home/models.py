from django.db import models
from api.models import Movies, Category
# Create your models here.
#class Movies(models.Model):
	#moviename = models.CharField(Movies.moviename)
	#language = models.CharField(max_length=50)